apiVersion: v1
kind: Service
metadata: { name: control-plane, namespace: helixworks-forge }
spec: { selector: { app.kubernetes.io/name: control-plane }, ports: [{ name: http, port: 8080, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: generator, namespace: helixworks-forge }
spec: { selector: { app.kubernetes.io/name: generator }, ports: [{ name: http, port: 8080, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: runtime, namespace: helixworks-forge }
spec: { selector: { app.kubernetes.io/name: runtime }, ports: [{ name: http, port: 8080, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: evidence, namespace: helixworks-forge }
spec: { selector: { app.kubernetes.io/name: evidence }, ports: [{ name: http, port: 8080, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: broker, namespace: helixworks-forge }
spec: { selector: { app.kubernetes.io/name: broker }, ports: [{ name: http, port: 8080, targetPort: http }] }
